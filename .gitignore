# Dependencies / build output
node_modules/
dist/
coverage/

# Local secrets — NEVER commit these
.dev.vars
.dev.vars.*
.env
.env.*
# ...but the placeholder template is part of the repo
!.dev.vars.example

# Wrangler local state (includes local D1/KV/R2 data)
.wrangler/

# OS / editor noise
.DS_Store
*.local
.idea/
