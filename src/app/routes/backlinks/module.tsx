import { ComingSoon } from "../../components/coming-soon";

/**
 * Owned wholesale by the Backlinks UI agent — replace with the module root per
 * docs/specs/PHASE2.md. Mounted at /app/backlinks/*.
 */
export function BacklinksModule() {
  return (
    <ComingSoon
      title="Backlinks"
      description="Any site's Domain Score, backlink profile, referring domains, anchors, and growth over time."
      phase={2}
    />
  );
}
