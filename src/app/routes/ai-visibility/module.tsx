import { ComingSoon } from "../../components/coming-soon";

/**
 * Owned wholesale by the AI Visibility UI agent — replace with the module
 * root per docs/specs/PHASE6.md. Mounted at /app/ai-visibility/*.
 */
export function AiVisibilityModule() {
  return (
    <ComingSoon
      title="AI Visibility"
      description="Track whether AI assistants mention and cite your site when people ask questions you care about."
      phase={6}
    />
  );
}
