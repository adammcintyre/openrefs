import { ComingSoon } from "../../components/coming-soon";

/**
 * Owned wholesale by the Site Audit UI agent — replace with the module root
 * per docs/specs/PHASE4.md. Mounted at /app/site-audit/*.
 */
export function SiteAuditModule() {
  return (
    <ComingSoon
      title="Site Audit"
      description="Crawl a site you own for the technical issues that hold rankings back."
      phase={4}
    />
  );
}
