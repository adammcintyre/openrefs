import { ComingSoon } from "../../components/coming-soon";

/**
 * Owned wholesale by the Search Console UI agent — replace with the module
 * root per docs/specs/PHASE5.md. Mounted at /app/search-console/*.
 */
export function SearchConsoleModule() {
  return (
    <ComingSoon
      title="Search Console"
      description="Connect Google Search Console to surface striking-distance keywords, low-CTR pages, and cannibalization."
      phase={5}
    />
  );
}
