import { ComingSoon } from "../../components/coming-soon";

/**
 * Owned wholesale by the Gap Analysis UI agent — replace with the module root
 * per docs/specs/PHASE2.md. Mounted at /app/gap-analysis/*.
 */
export function GapAnalysisModule() {
  return (
    <ComingSoon
      title="Gap Analysis"
      description="Keywords your competitors rank for that you don't — missing, weak, and untapped."
      phase={2}
    />
  );
}
