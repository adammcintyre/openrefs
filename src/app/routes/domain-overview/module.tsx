import { ComingSoon } from "../../components/coming-soon";

/**
 * Owned wholesale by the Domain Overview UI agent — replace this placeholder
 * with the module root per docs/specs/PHASE1.md. Mounted at
 * /app/domain-overview/* so all sub-routing happens inside this module.
 */
export function DomainOverviewModule() {
  return (
    <ComingSoon
      title="Domain Overview"
      description="Estimate any site's organic traffic, top keywords and pages, competitors, and country split."
      phase={1}
    />
  );
}
