import { ComingSoon } from "../../components/coming-soon";

/**
 * Owned wholesale by the Rank Tracking UI agent — replace with the module root
 * per docs/specs/PHASE3.md. Mounted at /app/rank-tracking/*.
 */
export function RankTrackingModule() {
  return (
    <ComingSoon
      title="Rank Tracking"
      description="Daily position checks for the keywords that matter to a site you own."
      phase={3}
    />
  );
}
