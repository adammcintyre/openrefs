/**
 * Public privacy policy at /privacy — a Google OAuth verification prerequisite
 * (docs/specs/PHASE5.md). Owned by the Phase 5 worker agent, which replaces
 * this stub with the real policy copy.
 */
export function Privacy() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-16">
      <h1 className="text-2xl font-semibold text-foreground">Privacy policy</h1>
      <p className="mt-4 text-sm text-muted-foreground">
        The full policy is being finalised. OpenRefs stores your account email,
        your encrypted API credentials, and usage metering — nothing more — and
        deleting your account removes all of it.
      </p>
    </main>
  );
}
